{
  "name": "pingstats-landing",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "astro build && wrangler versions upload"
  },
  "dependencies": {
    "astro": "^5.0.0"
  },
  "devDependencies": {}
}
